@{
  Layout = "_Layout";
}

@using Food.Models;
@model List<Food.Models.Restaurant>;

<h1>Restaurants</h1>

@if (@Model.Count == 0)
{
  <h3>No restaurants have been added yet!</h3>
} 

@foreach (Item item in Model)
{
  <li>@Html.ActionLink($"{restaurant.Description}", "Details", new { id = item.ItemId }) | @item.Category.Name</li>
}

<p>@Html.ActionLink("Add new item", "Create")</p>